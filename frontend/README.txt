Run "npm install" while in the frontend folder. You only need to do this once. It just installs dependencies

Run "npm start" to start the frontend server

Go to "http://127.0.0.1:8888/"

The frontend has hot reloading, so after changes to the files are changed. It will automatically recompile.
